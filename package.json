{
  "name": "js_scraper",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "wrangler dev --env dev --test-scheduled",
    "deploy": "wrangler types && wrangler deploy --env prod",
    "scheduled": "curl http://localhost:8787/__scheduled",
    "build": "npx tsc"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "axios": "1.11.0",
    "cheerio": "1.1.2",
    "wrangler": "4.38.0"
  },
  "devDependencies": {
    "@types/cheerio": "0.22.35",
    "@types/node": "24.3.1",
    "typescript": "5.9.2"
  }
}
